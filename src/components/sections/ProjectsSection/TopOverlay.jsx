import React from 'react';
import {
    wrap
} from './TopOverlay.module.scss';

const TopOverlay = () => {
    return (
        <div className={wrap}>
            
        </div>
    );
};

export default TopOverlay;